package dxc;

public class FirstThread extends Thread {

	/**
	 * @param args
	 */

	// TODO Auto-generated method stub
	// 多线程创建方法一：
	// 定义一个类，它继承自Thread并重写Thread类中的run方法，
	// 方法run()称为线程体，由于java只支持单继承，所以这种方法定义的类不能再继承
	// 自其它类了
	public void run() {
		for (int i = 0; i < 100; i++) {
			System.out.println("first thread"+i);
		}
	}

}
