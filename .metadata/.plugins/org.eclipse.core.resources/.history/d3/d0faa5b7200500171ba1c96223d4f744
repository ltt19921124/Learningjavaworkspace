package Ïß³Ì;

public class Sort {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		int[] arr = {4,6,78,34,56};
		for(int i=1;i<arr.length;i++){
			int temp = arr[i];
			for(int j=i-1;j>=0 && j < temp;j--){
				arr[j+1] = arr[j];
				arr[j] = temp;
			}
		}
		for(int i=0;i<arr.length;i++){
			System.out.println(arr[i]);
		}
	}

}
